# Replace Mistral OCR with PDF-Direct-to-LLM Pipeline

## High-Level Goal

Replace the Mistral OCR step ($30/day) in the paper processing pipeline by sending the PDF directly to Gemini 3 Flash via OpenRouter's `file` content type. Instead of PDF → Mistral OCR → markdown → LLM summary, we do PDF URL → Gemini 3 Flash → summary.

**Cost savings**: eliminates ~$900/month in Mistral OCR costs. Test results on a 144-page paper showed PDF-via-URL costs $0.0066/paper (vs $30/day OCR). At 1000 papers/day ≈ $6.60/day.

**Test results** (arxiv 2303.18223, 144 pages, 5.6 MB):

| Method | Cost | Time | Prompt tokens |
|--------|------|------|---------------|
| PDF base64 via `file` type | $0.0398 | 29.1s | 75,439 |
| PDF URL via `file` type | **$0.0066** | 36.4s | 75,439 |

Both produced high-quality summaries. PDF URL is 6x cheaper — use this approach.

**What we lose (accepted trade-offs)**:
- No more extracted figures with bounding boxes (to be revisited later)
- No more `final_markdown` (OCR text) stored per paper
- No more `content.md`, `sections.json`, `figures.json` in Supabase Storage
- No more inline image references in markdown

**What stays the same**:
- Metadata extraction (already uses page images from first 3 pages)
- Embedding generation (uses title + abstract)
- Abstract summary generation (uses abstract text)
- Thumbnail generation (uses first page image)

**Summary generation stays on-demand**: `generate_five_minute_summary` remains disabled in the worker pipeline. Papers continue to get `status=partially_completed`. The web service generates summaries on-demand when requested — the change is that it now sends the PDF URL (from DB) via `file` content type instead of downloading `content.md` text.

---

## File Tree

```
worker/
├── paperprocessor/
│   ├── client.py                              # MODIFY - remove OCR/header/image steps, clean imports
│   ├── models.py                              # MODIFY - remove ocr_markdown, images from ProcessedPage
│   └── internals/
│       ├── mistral_ocr.py                     #   KEEP (unused, available as fallback)
│       ├── header_formatter.py                #   KEEP (unused, available as fallback)
│       ├── pdf_to_image.py                    #   NO CHANGE (still converts first 3 pages for metadata + thumbnail)
│       ├── summary_generator.py               # MODIFY - send PDF URL via file content type, change model, add plugins
│       ├── metadata_extractor.py              #   NO CHANGE
│       ├── structure_extractor.py             #   KEEP (unused, needs ocr_markdown if re-enabled)
│       └── section_rewriter.py                #   KEEP (unused)
│       └── prompts/generate_5min_summary.md   #   NO CHANGE (prompt works as-is for PDF input)
├── papers/
│   ├── storage.py                             # MODIFY - stop uploading content.md/figures/sections, simplify to thumbnail + metadata only
│   └── client.py                              # MODIFY - remove figures, remove final_markdown, simplify _upload_to_storage
├── dags/
│   ├── paper_processing_worker_dag.py         # MODIFY - update doc_md, pass pdf_url
│   ├── backfill_summaries_dag.py              # MODIFY - handle empty final_markdown for new-format papers
│   ├── backfill_abstract_summaries_dag.py     # MODIFY - handle empty final_markdown fallback
│   ├── backfill_storage_dag.py                # DEPRECATE - no longer needed, old OCR storage migration
│   ├── backfill_final_markdown_dag.py         # DEPRECATE - no longer needed, final_markdown removed
│   └── compress_oversized_figures_dag.py      # DEPRECATE - no longer needed, figures no longer stored
└── shared/
    └── openrouter/client.py                   # MODIFY - add get_llm_response_with_file() for file+plugins support

web/src/
├── services/
│   ├── summary-generation.service.ts          # MODIFY - look up pdf_url from DB, send via file content type
│   └── papers.service.ts                      # MODIFY - getPaperJson/getPaperMarkdown handle missing content.md
├── lib/
│   ├── supabase/storage.ts                    # MODIFY - update deletePaperAssets, remove content.md/figures helpers
│   └── openrouter.ts                          # MODIFY - support file content type + plugins param
└── app/api/papers/[uuid]/
    └── markdown/route.ts                      # MODIFY - repurpose or deprecate

web/src/services/
└── papers.service.test.ts                     # MODIFY - update tests for new storage format

web/tests/integration/
└── papers.integration.test.ts                 # MODIFY - update tests for new storage format
```

---

## Per-File Methods and Types

### worker/paperprocessor/models.py (MODIFY)

#### Types

```
ProcessedPage {
  page_number: int
  img_base64: str
  width: int
  height: int
}
// REMOVED: ocr_markdown, structured_markdown, images list

ProcessedDocument {
  pdf_base64: str
  paper_uuid: Optional[str]
  arxiv_id: Optional[str]
  title: Optional[str]
  authors: Optional[str]
  pages: List[ProcessedPage]       // still first 3 pages for metadata + thumbnail
  five_minute_summary: Optional[str]
  abstract_summary: Optional[str]
  embedding: Optional[List[float]]
  abstract: Optional[str]
  step_costs: List[ApiCallCostForStep]
}
// REMOVED: headers, final_markdown, sections, rewritten_final_markdown, pdf_url
// KEPT: ProcessedImage, Header, Section classes (unused but not deleted)
```

---

### worker/paperprocessor/internals/pdf_to_image.py (NO CHANGE)

Still converts first 3 pages for metadata extraction and thumbnail. Keep existing `max_pages=3` default.

---

### worker/paperprocessor/internals/summary_generator.py (MODIFY)

#### Changes
- Change `SUMMARY_MODEL` from `"google/gemini-2.5-pro"` to `"google/gemini-3-flash-preview"` (matches web service)
- Send PDF via OpenRouter `file` content type with URL (not base64, not page images)
- Add `plugins: [{"id": "file-parser", "pdf": {"engine": "native"}}]` to avoid mistral-ocr fallback ($2/1000 pages)
- Requires new `get_llm_response_with_file()` in openrouter client, or pass plugins param

#### Methods

| Method | Arguments | Returns | Description |
|--------|-----------|---------|-------------|
| `generate_five_minute_summary` | `document: ProcessedDocument, pdf_url: str` | `None` (mutates document) | Build message with PDF URL via `file` content type (`{"type": "file", "file": {"filename": "paper.pdf", "file_data": pdf_url}}`). Send to Gemini 3 Flash with `native` PDF engine plugin. Store result in `document.five_minute_summary`. Currently not called from worker pipeline (on-demand via web). |
| `generate_abstract_summary` | `document: ProcessedDocument` | `None` (mutates document) | Already uses `document.abstract` text. Current fallback reads `final_markdown` and raises `RuntimeError` if both are missing. Replace the `raise` with `logger.warning` + `return` (skip abstract summary gracefully). |

---

### worker/shared/openrouter/client.py (MODIFY)

#### Methods

| Method | Arguments | Returns | Description |
|--------|-----------|---------|-------------|
| `get_llm_response_with_file` | `messages: List[Dict], model: str, plugins: Optional[List[Dict]]` | `LLMCallResult` | New method similar to `get_llm_response` but accepts optional `plugins` param for the request payload. Used for PDF file content type with `native` engine. |

---

### worker/paperprocessor/client.py (MODIFY)

#### Methods

| Method | Arguments | Returns | Description |
|--------|-----------|---------|-------------|
| `process_paper_pdf` | `pdf_contents: bytes, paper_id: Optional[str]` | `ProcessedDocument` | Simplified pipeline: (1) convert first 3 pages to images (metadata + thumbnail), (2) extract metadata, (3) generate abstract summary, (4) generate embedding. Remove OCR, header formatting, and image formatting steps. `generate_five_minute_summary` stays commented out (on-demand via web). |

---

### worker/papers/client.py (MODIFY)

#### Methods

| Method | Arguments | Returns | Description |
|--------|-----------|---------|-------------|
| `save_paper` | `db: Session, processed_content: ProcessedDocument` | `Paper` | Remove figures conversion loop (lines 472-490). Remove `final_markdown` from result_dict. Keep thumbnail from first page. |
| `_upload_to_storage` | `paper_uuid: str, result_dict: Dict` | `None` | Upload thumbnail + metadata.json with `pipeline_version: 2`. Stop uploading content.md, sections.json, figures.json. |
| `get_paper` | `db: Session, paper_uuid: str` | `Paper` | Update to handle missing `sections.json` and `figures.json` for new-format papers. Use `pipeline_version` from metadata to branch. |

---

### worker/papers/storage.py (MODIFY)

#### Methods

| Method | Arguments | Returns | Description |
|--------|-----------|---------|-------------|
| `upload_paper_assets` | `paper_uuid: str, thumbnail_bytes: bytes, metadata: dict` | `None` | Upload thumbnail + metadata.json with `pipeline_version: 2`. Stop uploading content.md, sections.json, figures.json. No PDF upload needed — URL is already in DB. |
| `download_paper_content` | `paper_uuid: str` | `StoredPaperContent` | Check `pipeline_version` from metadata first. For v2 papers: skip downloading `content.md`, `sections.json`, `figures.json` (they don't exist). Return empty string for `final_markdown`, empty lists for `sections` and `figures`. For v1 papers: keep existing behavior. |
| `delete_paper_assets` | `paper_uuid: str` | `None` | Update to handle both old format (content.md, figures, sections) and new format (just thumbnail + metadata). |

---

### worker/dags/paper_processing_worker_dag.py (MODIFY)

#### Changes

- Update `doc_md` string to reflect new pipeline (no OCR step)
- Pass `pdf_url` (arxiv URL or `job.pdf_url`) to `process_paper_pdf` if needed for future use

---

### web/src/services/summary-generation.service.ts (MODIFY)

#### Methods

| Method | Arguments | Returns | Description |
|--------|-----------|---------|-------------|
| `generateSummaryForPaper` | `paperUuid: string` | `GenerateSummaryResult` | For v2 papers: look up `pdf_url` or construct arxiv PDF URL from `arxiv_id` in DB. Send via `file` content type to Gemini 3 Flash with `native` engine plugin. For v1 papers: keep existing text-based approach (download content.md). |

---

### web/src/lib/supabase/storage.ts (MODIFY)

#### Methods

| Method | Arguments | Returns | Description |
|--------|-----------|---------|-------------|
| `downloadPaperContent` | `paperUuid: string` | `Promise<StoredPaperContent>` | Check `pipeline_version` from metadata. For v2 papers: skip `content.md`, `sections.json`, `figures.json` downloads. Return empty values. For v1: keep existing behavior. |
| `downloadPaperMarkdown` | — | — | Deprecate or make it return empty string for backwards compatibility. |
| `deletePaperAssets` | `paperUuid: string` | `Promise<void>` | Update to handle both old format and new format (just thumbnail + metadata). |

---

### web/src/lib/openrouter.ts (MODIFY)

#### Types

```
FilePart = { type: "file", file: { filename: string, file_data: string } }
ContentPart = { type: "text", text: string } | { type: "image_url", image_url: { url: string } } | FilePart

OpenRouterMessage {
  role: string
  content: string | ContentPart[]   // was: string only
}
```

#### Methods

| Method | Arguments | Returns | Description |
|--------|-----------|---------|-------------|
| `chatCompletion` | `messages: OpenRouterMessage[], model: string, plugins?: Plugin[]` | `ChatCompletionResult` | Extend `OpenRouterMessage.content` type to support multimodal + file content. Add optional `plugins` param for PDF engine selection. |

---

### web/src/services/papers.service.ts (MODIFY)

#### Methods

| Method | Arguments | Returns | Description |
|--------|-----------|---------|-------------|
| `getPaperJson` | `paperUuid: string` | `PaperJson` | Currently downloads `content.md`, `sections.json`, `figures.json`. Must handle new-format papers that have none of these — check `pipeline_version` from metadata. |
| `getPaperMarkdown` | `paperUuid: string` | `string` | Delegates to `downloadPaperMarkdown`. Return empty string for new-format papers. |

---

### worker/dags/backfill_summaries_dag.py (MODIFY)

#### Changes

- Uses `storage.download_paper_content()` which returns `final_markdown`. Must handle case where `final_markdown` is empty (new-format papers).

---

### worker/dags/backfill_abstract_summaries_dag.py (MODIFY)

#### Changes

- Line 84-86 reads `stored.final_markdown` as fallback. Must handle empty/missing `final_markdown` for new-format papers.

---

## Important Technical Notes

### OpenRouter file content type format
The correct format for sending PDFs via OpenRouter:
```json
{
  "type": "file",
  "file": {
    "filename": "paper.pdf",
    "file_data": "https://arxiv.org/pdf/2303.18223"
  }
}
```
- Field name is `file_data` (not `data` or `url`)
- Accepts either a URL string or a `data:application/pdf;base64,...` data URI
- URL is 6x cheaper than base64 for the same token count — always prefer URLs
- Must include `plugins: [{"id": "file-parser", "pdf": {"engine": "native"}}]` in the request to avoid fallback to mistral-ocr ($2/1000 pages)

### PDF URL sources — no Supabase PDF storage needed
The PDF URL is already available from the database:
- **Arxiv papers**: construct from `arxiv_id` → `https://arxiv.org/pdf/{arxiv_id}`
- **Non-arxiv papers**: `pdf_url` column already exists in the `papers` DB table (`worker/papers/db/models.py:40`)
- No need to store PDFs in Supabase Storage — the original source URLs are permanent and publicly accessible

### Storage schema change
Old: `{uuid}/content.md`, `{uuid}/figures/{id}.png`, `{uuid}/figures.json`, `{uuid}/sections.json`, `{uuid}/thumbnail.png`, `{uuid}/metadata.json`
New: `{uuid}/thumbnail.png`, `{uuid}/metadata.json`

Existing papers in storage keep their old format. Use `pipeline_version` field in `metadata.json` to distinguish formats: version 2 = PDF-direct pipeline, absent/version 1 = old OCR format.

### Backwards compatibility for existing papers
~All existing papers already have summaries. The on-demand summary service only runs for papers with `status=partially_completed`. New papers will use the PDF pipeline. For any edge case where an old paper needs re-summarization, the text fallback in the web service handles it.

### Abstract summary fallback
`generate_abstract_summary` currently falls back to first 1000 words of `final_markdown` if no abstract is available. Since `final_markdown` will no longer exist, this fallback must change. Replace the `raise RuntimeError` with `logger.warning` + `return` (skip abstract summary gracefully). Most arxiv papers have abstracts from arxiv metadata.

### No more page image conversion for summaries
The previous plan required converting all PDF pages to images. With the PDF URL approach, this is no longer needed. `pdf_to_image.py` still converts the first 3 pages for metadata extraction and thumbnail — no changes needed there.

### Memory and payload benefits
- No need to hold 70+ page images in memory
- No need to upload PDFs or page images to Supabase Storage
- Request payload is tiny (just a URL string vs megabytes of base64 images)
- Supabase storage per paper drops from ~10MB (content.md + figures) to ~100KB (just thumbnail + metadata)

---

## Phases

### Phase 1: Worker pipeline + storage (must deploy together)

- [ ] Update `ProcessedPage` in `models.py`: remove `ocr_markdown`, `structured_markdown`, `images` fields
- [ ] Update `ProcessedDocument` in `models.py`: remove `headers`, `final_markdown`, `sections`, `rewritten_final_markdown` fields
- [ ] Add `get_llm_response_with_file()` to `shared/openrouter/client.py` (or add `plugins` param to existing `get_llm_response`)
- [ ] Update `summary_generator.py`: change `SUMMARY_MODEL` to `"google/gemini-3-flash-preview"`
- [ ] Update `summary_generator.py`: make `generate_five_minute_summary` accept `pdf_url` param and send via `file` content type with `native` engine plugin
- [ ] Update `summary_generator.py`: change `generate_abstract_summary` fallback — replace `raise RuntimeError` with `logger.warning` + `return`
- [ ] Update `client.py` (paperprocessor): remove OCR call, remove `format_headers`, remove `format_images`, clean unused imports, keep `generate_five_minute_summary` commented out
- [ ] Update `storage.py`: simplify `upload_paper_assets` to only upload thumbnail + metadata.json (with `pipeline_version: 2`). Stop uploading content.md, sections.json, figures.json
- [ ] Update `storage.py`: update `delete_paper_assets` to handle new paths
- [ ] Update `storage.py`: update `download_paper_content` to work without content.md for v2 papers
- [ ] Update `papers/client.py`: remove figures conversion in `save_paper`, remove `final_markdown` from result_dict
- [ ] Update `papers/client.py`: simplify `_upload_to_storage` to only upload thumbnail + metadata
- [ ] Update `papers/client.py`: update `get_paper` to handle missing sections.json/figures.json for new-format papers

### Phase 2: Web service changes

- [ ] Update `web/src/lib/openrouter.ts`: extend content types to support `file` part + add optional `plugins` param
- [ ] Update `web/src/services/summary-generation.service.ts`: look up `pdf_url`/`arxiv_id` from DB, construct PDF URL, send via `file` content type to Gemini 3 Flash with `native` engine plugin. Check `pipeline_version` for backward compat.
- [ ] Update `web/src/lib/supabase/storage.ts`: update `deletePaperAssets` and `downloadPaperContent` for new format
- [ ] Update `web/src/services/papers.service.ts`: handle new-format papers in `getPaperJson` and `getPaperMarkdown`
- [ ] Deprecate or update `web/src/app/api/papers/[uuid]/markdown/route.ts`
- [ ] Update `web/src/services/papers.service.test.ts`: update tests for new storage format
- [ ] Update `web/tests/integration/papers.integration.test.ts`: update tests for new storage format

### Phase 3: DAGs and cleanup

- [ ] Update `paper_processing_worker_dag.py`: update doc_md description
- [ ] Update `backfill_summaries_dag.py`: handle empty `final_markdown` for new-format papers
- [ ] Update `backfill_abstract_summaries_dag.py`: handle empty `final_markdown` fallback
- [ ] Deprecate `backfill_storage_dag.py`: add deprecation notice, this DAG is no longer needed
- [ ] Deprecate `backfill_final_markdown_dag.py`: add deprecation notice, final_markdown no longer exists for new papers
- [ ] Deprecate `compress_oversized_figures_dag.py`: add deprecation notice, figures no longer stored for new papers
- [ ] Test end-to-end with a real paper through the DAG
- [ ] Verify existing papers still work (backwards compatibility)
- [ ] Monitor costs for first 24 hours after deployment

---

## Success Criteria

1. New papers process without calling Mistral OCR API
2. Summaries generated from PDF URL are comparable quality to OCR-based summaries
3. Mistral OCR daily spend drops to $0
4. On-demand summary generation works for both new papers (PDF URL from DB) and old papers (text fallback)
5. No regression in metadata extraction, embedding generation, or abstract summaries
6. Processing cost per paper < $0.01 (confirmed: $0.0066 for 144-page paper)
7. Supabase storage per paper reduced to ~100KB (thumbnail + metadata only)
