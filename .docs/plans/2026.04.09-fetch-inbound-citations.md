# Fetch Inbound Citations DAG

## High-Level Goal

Add a new Airflow DAG (`fetch_inbound_citations`) that fetches "papers that cite our papers" from the Semantic Scholar citations batch API. This enriches the citation graph with inbound edges -- currently we only have outbound references from our papers.

The key constraint is **preventing exponential graph growth**. A strict two-hop boundary must be enforced:

1. **Hop 0 (our papers)**: Outbound references fetched by `citation_graph` DAG. Inbound citations fetched by the **new** `fetch_inbound_citations` DAG.
2. **Hop 1 (external nodes)**: Outbound references fetched by `expand_external_references` DAG. No inbound citations fetched.
3. **Hop 2+**: Nothing fetched. The graph stops growing.

**Design decision: fetch-once.** Inbound citations are fetched once per paper. The `papers.inbound_citations_fetched_at` column tracks completion. New citations that appear after the initial fetch are not picked up. This mirrors the outbound reference behavior (tracked via sentinel rows in `paper_citations`).

### Audit of `expand_external_references` DAG

The existing `expand_external_references` DAG fetches outbound references for any `cited_s2_id` that has never appeared as a `source_s2_id`. When the new inbound citations DAG introduces new citing papers as `source_s2_id` entries, the question is whether this creates runaway growth.

**This is safe because:**
- The expand DAG only fetches **outbound references** (not inbound citations) for external nodes.
- Once an external node's outbound references are fetched, it gets a `source_s2_id` entry (real edges or sentinel), so it won't be fetched again.
- Citing papers introduced by inbound edges appear as `source_s2_id`, NOT as `cited_s2_id`. The expand DAG's query (`fetch_external_nodes_needing_references`) finds nodes that appear as `cited_s2_id` but NOT as `source_s2_id`. So citing-only papers are **not** eligible for expansion.
- A citing paper X only gets expanded if X also appears as `cited_s2_id` somewhere (i.e., some other paper references X). This happens naturally when our papers or expanded external nodes cite X.
- The inbound citations DAG **only queries papers in the `papers` table**, not external nodes. This is the critical boundary that prevents exponential growth.

**Growth analysis:** If we ingest N papers/day and each has ~K inbound citations, the inbound DAG adds ~N*K new source nodes per week. The expand DAG only processes the subset of these that also appear as `cited_s2_id` targets, which is a fraction. Growth is linear in N, not exponential.

---

## File Tree

```
worker/
  shared/
    semantic_scholar/
      client.py                          # MODIFIED - add fetch_paper_citations_batch()
      models.py                          # MODIFIED - add S2PaperCitation dataclass
  papers/
    db/
      models.py                          # MODIFIED - add inbound_citations_fetched_at column to PaperRecord
  dags/
    citation_helpers.py                  # MODIFIED - add _bulk_insert_edges(), fetch_and_store_inbound_citations(), refactor fetch_and_store_references()
    fetch_inbound_citations_dag.py       # NEW - the new DAG
    expand_external_references_dag.py    # UNCHANGED (audit confirms it's safe)
    citation_graph_dag.py                # UNCHANGED
  tests/
    test_citation_graph.py               # MODIFIED - add tests for new functions
  migrations/
    versions/
      xxxxxxxx_add_inbound_citations_fetched_at.py  # NEW - add column to papers table
```

---

## Tests

All tests go in `worker/tests/test_citation_graph.py`, following the existing pattern.

- **[unit]** `test_fetch_paper_citations_batch`: Verify `fetch_paper_citations_batch` parses S2 batch response into `S2PaperCitation` DTOs correctly -- handles null entries, null citingPaper.paperId, and correlates `cited_s2_id` from input order. Uses `@patch('shared.semantic_scholar.client._request_with_retry')`. Functions: `fetch_paper_citations_batch` in `client.py`.

- **[unit]** `test_fetch_and_store_inbound_citations_inserts_edges`: Verify `fetch_and_store_inbound_citations` inserts edges with correct direction (citing_paper becomes `source_s2_id`, our paper becomes `cited_s2_id`) and sets `inbound_citations_fetched_at` on processed papers. Uses `@patch('citation_helpers.fetch_paper_citations_batch')` with a mock session. Functions: `fetch_and_store_inbound_citations` in `citation_helpers.py`.

- **[unit]** `test_fetch_and_store_inbound_citations_zero_citers`: Verify that papers with 0 inbound citations still get `inbound_citations_fetched_at` set (no sentinel row needed -- the timestamp is the completion marker). Functions: `fetch_and_store_inbound_citations` in `citation_helpers.py`.

- **[unit]** `test_fetch_papers_needing_inbound_citations`: Verify `fetch_papers_needing_inbound_citations` returns only papers in the `papers` table that have an `s2_paper_id` and `inbound_citations_fetched_at IS NULL`. Uses mock session. Functions: `fetch_papers_needing_inbound_citations` in `fetch_inbound_citations_dag.py`.

- **[unit]** `test_fetch_papers_needing_inbound_citations_idempotent`: Verify that a second run returns an empty list when all papers already have `inbound_citations_fetched_at` set. Uses mock session. Functions: `fetch_papers_needing_inbound_citations` in `fetch_inbound_citations_dag.py`.

- **[unit]** `test_expand_dag_does_not_fetch_inbound_only_nodes`: Verify that `fetch_external_nodes_needing_references` does NOT return papers that only appear as `source_s2_id` from inbound citation edges (they don't appear as `cited_s2_id`). This confirms the expand DAG boundary. Functions: `fetch_external_nodes_needing_references` in `expand_external_references_dag.py`.

---

## Important Technical Notes

### Tracking inbound fetch completion: `papers.inbound_citations_fetched_at`

A nullable `TIMESTAMPTZ` column on the `papers` table. Set to `NOW()` when the DAG successfully fetches inbound citations for a paper (regardless of whether 0 or 1000 citers were found). The query for papers needing fetch is:

```sql
SELECT DISTINCT p.s2_ids->>'s2_paper_id' AS s2_paper_id
FROM papers p
WHERE p.s2_ids->>'s2_paper_id' IS NOT NULL
  AND p.s2_ids->>'s2_paper_id' != ''
  AND p.inbound_citations_fetched_at IS NULL
```

This is simpler and more explicit than sentinel rows. No changes needed to existing queries in `citation_graph_dag.py` or `expand_external_references_dag.py` since `source_s2_id` stays NOT NULL.

Note: outbound references continue to use sentinel rows in `paper_citations` (`source=our_paper, cited=NULL`) for tracking -- that system is well-tested and adding a redundant column would create two sources of truth.

**Crash safety:** `inbound_citations_fetched_at` is only set after all edges for a batch are committed. If the process crashes mid-batch, those papers still have `NULL` and will be re-fetched on the next run. Re-inserting already-stored edges is safe due to `ON CONFLICT DO NOTHING`.

### S2 Citations Batch API

The S2 batch endpoint `POST /paper/batch` with `fields=citations.paperId` returns citations (papers that cite the queried papers). The batch endpoint truncates at **1000 citations per paper** for nested fields. For highly-cited papers, this means we get a partial list. This is acceptable -- the goal is to enrich the graph, not to be exhaustive. There is no way to detect truncation from the response.

The response structure mirrors references:

```json
[
  { "citations": [{"paperId": "abc123"}, {"paperId": "def456"}] },
  null,
  { "citations": [{"paperId": null}] }
]
```

Note: `isInfluential` is not available via the batch endpoint for nested citation/reference fields (same limitation as the existing `fetch_paper_references_batch`). The `S2PaperCitation.is_influential` field will always be `None` from batch calls.

### Bulk INSERT optimization for `fetch_and_store_references` and `fetch_and_store_inbound_citations`

The existing `fetch_and_store_references` in `citation_helpers.py` does row-by-row INSERTs -- one `session.execute()` per edge. For a batch of 500 papers with ~30 refs each, that's ~15,000 individual INSERT statements per batch. This is slow and will be worse for the inbound DAG where popular papers can have hundreds of citers.

Both functions should use a bulk INSERT approach: build a VALUES list for all edges in a batch and execute a single `INSERT ... VALUES (...), (...), ... ON CONFLICT DO NOTHING` statement. This matches the pattern already used by `write_paper_scores` in `citation_graph_dag.py` (lines 216-227). Sentinel rows (one per paper with 0 results) remain as individual inserts since there are few of them.

### Preventing re-expansion of inbound-only nodes

When inbound citations are stored, the citing papers become `source_s2_id` entries. The expand DAG's `fetch_external_nodes_needing_references` query finds nodes that appear as `cited_s2_id` but NOT as `source_s2_id`. Citing papers that appear ONLY as `source_s2_id` (from inbound edges) are NOT eligible for expansion -- they need to also appear as `cited_s2_id` somewhere to be picked up. This means:

- If paper X cites our paper P, the edge `(source=X, cited=P)` is stored. X appears as `source_s2_id` only. The expand DAG does NOT pick up X.
- X only gets expanded if some other paper also cites X (X appears as `cited_s2_id`). This happens naturally when our papers or expanded external nodes have X in their references.
- This naturally limits growth without any additional logic.

---

## Per-File Methods and Types/DTOs

### models.py -- semantic_scholar (MODIFIED)

#### Types

```
S2PaperCitation {
  cited_s2_id: str           # The paper that was queried (our paper, the one being cited)
  citing_s2_id: str          # The paper that cites it (the citing paper)
  is_influential: Optional[bool]  # Always None from batch API
}
```

### models.py -- papers/db (MODIFIED)

- Add `inbound_citations_fetched_at = Column(DateTime(timezone=True), nullable=True)` to `PaperRecord`

### client.py (MODIFIED)

#### Methods

| Method | Arguments | Returns | Description |
|--------|-----------|---------|-------------|
| `fetch_paper_citations_batch` | `s2_paper_ids: List[str]` | `List[S2PaperCitation]` | Batch fetch citations (papers that cite these papers) from S2. 500 per request. Skips null entries and null citingPaper.paperId. Uses `fields=citations.paperId`. |

### citation_helpers.py (MODIFIED)

#### Methods

| Method | Arguments | Returns | Description |
|--------|-----------|---------|-------------|
| `_bulk_insert_edges` | `session: Session, edges: List[Tuple[str, str, Optional[bool]]]` | `None` | Bulk INSERT citation edges using a single VALUES list. Each tuple is `(source_s2_id, cited_s2_id, is_influential)`. Uses ON CONFLICT DO NOTHING. |
| `fetch_and_store_inbound_citations` | `session: Session, s2_paper_ids: List[str]` | `Dict[str, int]` | Batch-fetch inbound citations from S2 in chunks of `BATCH_SIZE` (500). Uses `_bulk_insert_edges` for edge insertion. Sets `inbound_citations_fetched_at` on each processed paper. Returns counts dict with `edges_inserted`, `papers_marked`, `errors`, `batches_processed`. |

Note: `fetch_and_store_references` is also refactored to use `_bulk_insert_edges` instead of row-by-row inserts.

### fetch_inbound_citations_dag.py (NEW)

DAG that runs weekly on Tuesday at 08:00 UTC. Single task: find papers needing inbound citation fetch, call `fetch_and_store_inbound_citations`.

#### Methods

| Method | Arguments | Returns | Description |
|--------|-----------|---------|-------------|
| `fetch_papers_needing_inbound_citations` | `session: Session` | `List[str]` | Query papers with `s2_paper_id` where `inbound_citations_fetched_at IS NULL`. Only queries the `papers` table -- never external nodes. |

### Migration (NEW)

#### Changes

- Add `inbound_citations_fetched_at` nullable `TIMESTAMPTZ` column to `papers` table

---

## Phases

### Phase 1: Schema Migration :white_check_mark:

- [x] Create Alembic migration: add `inbound_citations_fetched_at` column to `papers`
- [x] Update `PaperRecord` in `worker/papers/db/models.py`: add the new column

### Phase 2: S2 Client -- Citations Batch Function :white_check_mark:

- [x] Add `S2PaperCitation` dataclass to `models.py`
- [x] Add `fetch_paper_citations_batch()` to `client.py` (mirrors `fetch_paper_references_batch` but uses `fields=citations.paperId`)
- [x] Write unit test `test_fetch_paper_citations_batch`

### Phase 3: Bulk INSERT Optimization + Inbound Storage :white_check_mark:

- [x] Add `_bulk_insert_edges()` helper to `citation_helpers.py` (single INSERT with VALUES list + ON CONFLICT DO NOTHING)
- [x] Refactor `fetch_and_store_references()` to use `_bulk_insert_edges()` instead of row-by-row inserts
- [x] Add `fetch_and_store_inbound_citations()` to `citation_helpers.py` (uses `_bulk_insert_edges` and `BATCH_SIZE` chunking loop, sets `inbound_citations_fetched_at`)
- [x] Write unit tests for edge insertion and zero-citers behavior

### Phase 4: New DAG :white_check_mark:

- [x] Create `fetch_inbound_citations_dag.py` with `fetch_papers_needing_inbound_citations()` helper and single task
- [x] Schedule: weekly Tuesday 08:00 UTC (Monday=expand, Tuesday=inbound, daily 11:00=pagerank)
- [x] Write unit tests for `fetch_papers_needing_inbound_citations` including idempotency

### Phase 5: Safety Verification :white_check_mark:

- [x] Write test `test_expand_dag_does_not_fetch_inbound_only_nodes` confirming expand DAG boundary
- [x] Run full test suite

---

## Success Criteria

1. New DAG `fetch_inbound_citations` appears in Airflow and runs successfully
2. Inbound citation edges are stored in `paper_citations` as regular edges `(source=citing_paper, cited=our_paper)`
3. Papers get `inbound_citations_fetched_at` set after processing, regardless of citer count
4. The DAG only fetches inbound citations for papers in the `papers` table, never for external nodes
5. The expand DAG continues to work correctly and does not process inbound-only citing papers
6. No exponential graph growth: the two-hop boundary is enforced
7. No changes needed to existing DAG queries (`source_s2_id` stays NOT NULL)
8. All tests pass
9. Existing outbound reference pipeline is unaffected
